export default {
    setColor(state, color) {
        state.color = color;
    },
    setWidth(state, width) {
        state.width = width;
    },
    setAngle(state, angle) {
        state.angle = angle;
    }
};